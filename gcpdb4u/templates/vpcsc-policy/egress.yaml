- egressTo:
    operations:
    - serviceName: storage.googleapis.com
      methodSelectors:
      - method: 'google.storage.objects.list'
      - method: 'google.storage.buckets.testIamPermissions'
      - method: 'google.storage.objects.get'
      - method: 'google.storage.objects.create'
    - serviceName: containerregistry.googleapis.com
      methodSelectors:
      - method: 'containers.registry.read'
    resources:
    ***REMOVED*** Databricks owned projects(target) to which egress traffic from your project(source) is allowed to
    ***REMOVED*** multiple entries could be added with additional - projects/project_number entries
    - projects/643670579914 ***REMOVED***databricks global runtime artifact
    - projects/68422481410 ***REMOVED***databricks regional control plane

  egressFrom:
    identityType: ANY_IDENTITY